"use client"

import { useParams } from "next/navigation";
import { FC } from "react";

const FolderInfo: FC = () => {
    
    const {id} = useParams();
    
    return (
        <>
        
        </>
    )
}

export default FolderInfo;