"use client"

import { useParams } from "next/navigation";
import { FC } from "react";

const DocInfo: FC = () => {
    
    const {id} = useParams();
    
    return (
        <>
        
        </>
    )
}

export default DocInfo;